import React from 'react'

const Props = ({username,User,arr}) => {
    // console.log(username)
    console.log(User)

  return (
    <>
    Props Example
    <h2>My Name is {username} {User} {arr} </h2>
    </>
  )
}

export default Props